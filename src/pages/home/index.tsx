import { Header } from "components/Header";
import { PhotoGrid } from "components/PhotoGrid";

export const Home = () => {
  return (
    <>
      <Header></Header>
      <PhotoGrid></PhotoGrid>
    </>
  );
};
